# Ootique 상태

업데이트: 2026-08-07

## 지금 상태 한 줄

공개 투표 신고 기능(C단계 UGC 첫 항목)이 8/7에 서버·프리뷰까지 반영됐다. 실사용자 페이지(v.html) 교체만 lodim 지시 대기.

## 프리뷰 자동 반영 규칙 (2026-08-07 lodim 승인, 상시)

코딩이 끝나면 lodim의 배포 지시 없이 여기까지 자동으로 반영한다 — ① 추가 전용 DB 마이그레이션(`db push`) ② 하위호환 서버 함수(`functions deploy`) ③ 프리뷰 페이지 `docs/v-dev.html`(맨 위 보라색 `프리뷰 (테스트용)` 띠). lodim은 https://lodimsign.github.io/ootique/v-dev.html 에서 결과를 바로 본다.
**실사용자 페이지(v.html) 교체와 앱 스토어 배포는 여전히 lodim 지시가 필요하다.** LB의 develop 자동 배포와 같은 원리다.

공개 투표·순위 A단계를 8/6에 시작했다. 실행 명세는 `docs/friend-flow-scenario.md`의 `A단계 실행 스펙` 절이고, 서버 절반(마이그레이션·`vote` 함수·`vote-check.mjs`)을 먼저 만든 뒤 앱 절반을 붙인다. 안드로이드 공유 이미지 버그는 고쳐서 커밋했고 폰 확인만 남았다.

## 완료

- iPhone Expo Go 호환을 위해 Expo SDK 54로 전환
- 날짜 기반 오늘의 컬러와 6자리 친구 코드 규칙
- 카메라·앨범, 기기 내부 사진 보관, 최근 7일 기록
- 혼자 및 친구 A/B 공유 카드와 iOS 기본 공유 시트
- 개별 기록 및 모든 로컬 데이터 삭제
- App Store 메타데이터와 개인정보·지원 페이지 초안
- 안드로이드 하단 시스템 영역과 탭바가 겹치지 않도록 안전영역 적용
- 안드로이드 실기기에서 앱 탭바와 시스템 버튼이 겹치지 않는 것을 확인
- `expo-iap` 5.0.0 설치와 설정 플러그인 적용
- `com.lodim.ootique.plus` 비소모성 1회 구매·복원·앱 재실행 권한 판정 구현
- 무료 사용자는 오늘 포함 최근 7일만 남기고 8일째부터 사진과 기록을 안전하게 정리하며 Plus 사용자는 전체 로컬 기록 보관
- `assets/splash-icon.png`를 사용하는 iOS 스플래시 설정과 iPhone StoreKit 검증용 EAS `device` 내부 빌드 프로필
- App Store 앱·IAP 메타데이터, 첫 비소모성 상품 동시 제출 조건, 앱/IAP 심사용 스크린샷 목록 정리
- 번들 ID·빌드 번호·1024px 아이콘/스플래시·EAS 실기기/production 프로필·개인정보/지원 앵커·Plus 상품 ID를 한 번에 확인하는 `npm run check:release`
- 친구 코드 생성 성공 시 클립보드 자동 복사와 메신저 붙여넣기 안내
- 6자리 전체 표시, `6/6` 확인, 앱 내부 `코드 붙여넣기` 버튼
- 필수 규정 문서 정립 (`docs/spec.md`, `docs/git-strategy.md`, `docs/decisions.md`)
- 실행 명령과 통과 기준을 붙인 제작 매뉴얼 (`docs/app-development-manual.md`), 재사용용 애플 출시 매뉴얼은 `C:\workspace\260724\REALM\자료\애플 앱스토어 출시 매뉴얼.md`
- `App.tsx` 1,200줄을 592줄로 줄이고 화면·공용 조각을 `src/components/`와 `src/theme.ts`로 분리 (ADR-005~006)

## 다음 작업

1. ~~Note20에서 `이미지로 공유하기` 이미지 첨부 확인~~ — 2026-08-06 실기기 확인 완료. `expo-sharing` 교체가 맞았다.
2. ~~A단계 서버 배포와 검사~~ — 2026-08-06 완료. `db push`, `functions deploy vote`, `npm run test:vote` 전부 통과. 기존 `test:friend-sync:e2e`도 회귀 없이 통과.
3. ~~에뮬레이터 Expo Go로 공개 스위치 → 저장 → 링크 공유 화면 확인~~ — 2026-08-06 완료. 아래 `알려진 점`에 결과를 적었다.
4. 공개 사진이 실제로 서버에 올라가기 시작하는 시점에 `docs/index.html` 처리방침과 `docs/app-store-metadata.md` 개인정보 표시를 같이 고친다. **기능보다 먼저 고치면 사실과 다르고, 늦게 고치면 허위 표시다.**
5. B단계 순위 탭, C단계 UGC 나머지 3종(차단·처리 절차·무관용 EULA). ~~신고~~ — 2026-08-07 완료: `vote_reports` 테이블(같은 기기 중복 1회, 3회 자동 숨김·삭제 없음) + `vote` 함수 `action=report` + 프리뷰 `v-dev.html` 신고 UI + `test:vote` 신고 검사 8건 실서버 통과. v.html 교체는 lodim 확인 뒤.
6. 두 기기에서 코드 생성 → 참가 승인 → 각자 업로드 → 상대 사진 자동 표시를 수동 확인한다. **B 업로드와 PNG 스크린샷·고해상도 원본까지 본다**. 절차는 `docs/app-development-manual.md` 2.2절.
7. App Store Connect에 `com.lodim.ootique.plus` 비소모성 상품을 만들고 4,900원 가격대로 설정한다.
8. 구매 성공·취소·실패·보류·복원·앱 재실행과 8일째 삭제를 개발 빌드에서 확인한다.
9. Stitch 최종 디자인에서 로고, 룰렛 자산, 색상 토큰을 반영한다.
10. 최종 1024×1024 아이콘과 스플래시 자산으로 `assets/`를 교체한다.
11. iPhone 실기기에서 카메라, 사진 선택, 앱 재실행 후 기록, 공유 이미지를 확인한다.
12. Expo·Apple 로그인과 2FA 후 EAS production build 및 App Store 제출을 진행한다.

## 알려진 점

- **2026-08-06 화면 확인**: 안드로이드 에뮬레이터(API 36) Expo Go로 공개 플로우를 처음부터 끝까지 봤다. 컬러 뽑기 → 앨범 사진 → `오늘의 투표에 공개` 켜기 → 저장 → 공유 카드까지 통과했고, `친구에게 보내기`의 공유 시트에 문구와 `v.html?t=...` 링크가 함께 실렸다. `카드 이미지 공유`는 이미지 시트를 연 뒤 `링크도 복사했어요` 안내를 띄운다. 그 토큰으로 `action=card`가 JSON을, `action=img`가 `image/jpeg` 1,922바이트를 돌려줬고, `공개 내리기` 뒤에는 같은 토큰이 `404 not_found`가 됐다. 검사용 사진이라 서버에 남기지 않았다.
- 에뮬레이터 앨범은 파일을 넣어도 바로 안 보인다. `adb push`로 `/sdcard/DCIM/Camera/`에 넣고 `MEDIA_SCANNER_SCAN_FILE` 브로드캐스트를 보낸 뒤 사진 선택기를 다시 열어야 뜬다.
- 기본 Expo 아이콘은 임시이며 제출 전에 반드시 교체해야 한다.
- Expo SDK 54 권장 버전인 `react-native-safe-area-context` 5.6을 적용했다. 타입 검사, 도메인 검사, Expo Doctor 18/18, 안드로이드 번들 생성과 실기기 화면 확인을 통과했다.
- 2026-08-03 기준 iPhone App Store의 Expo Go가 지원하는 SDK 54로 전환했다. 의존성 검사, 타입 검사, 도메인 검사, Expo Doctor 18/18, iOS 번들 생성을 통과했다.
- `npm audit`은 Expo 54 빌드 도구에서 high 1건(`postcss`)과 moderate 10건을 보고한다. 자동 강제 수정은 SDK 57로 되돌리므로 적용하지 않았다.
- **2026-08-06 수정**: 안드로이드에서 `이미지로 공유하기`가 글자만 보내던 원인은 React Native `Share.share`의 `url`이 iOS 전용이기 때문이었다. RN 0.81 소스 주석이 안드로이드 지원 항목을 `title`과 `message`로만 적고 있다. `expo-sharing`을 설치해 `Sharing.shareAsync`로 한 경로로 합쳤고 두 OS 모두 파일이 첨부된다. 대신 iOS에서 같이 가던 안내 문구는 빠진다.
- **2026-08-06 결정**: 공유에는 앱으로 들어오는 링크를 무조건 같이 보낸다. `Sharing.shareAsync`는 텍스트를 못 싣고 안드로이드 메신저는 이미지에 붙인 텍스트를 대체로 버리므로, 기본 버튼은 `Share.share({ message })`로 문구와 링크만 보내고 카드 이미지는 링크 페이지의 OG 태그로 미리 보이게 한다. 이미지 파일 공유는 보조 버튼으로 남긴다. QR은 새 의존성이 필요해 넣지 않는다. 규칙은 `docs/app-development-manual.md` 규칙 6.
- **2026-08-06 결정**: 친구 A/B 투표 링크는 두 사람이 모두 공개를 켰을 때만 만들어진다. 한쪽만 켜면 그 사람 사진 한 장만 순위에 오르고 A/B 링크 버튼은 잠긴다. 동의하지 않은 사진이 링크로 나가는 경로를 두지 않기 위해서다.
- 수익 모델은 무료 최근 7일 기록과 `Ootique Plus` 4,900원 1회 구매다. 서버·DB·계정 없이 Apple 영수증으로 구매 권한만 복원하며, 앱 삭제나 기기 변경 뒤 기존 사진은 복원하지 않는다.
- 결제 라이브러리는 Expo 공식 안내에 있는 `expo-iap` 5.0.0을 설치했다. 설정 플러그인, 타입 검사, 도메인 검사, Expo Doctor 18/18을 통과했다.
- 상품 ID는 `com.lodim.ootique.plus`로 확정했다. 구매 성공 뒤 비소모성 거래를 완료하고, 취소·보류·실패·이미 구매함을 구분하며 구매 복원과 앱 시작 시 현재 구매 내역 재확인을 지원한다.
- 결제 구현은 타입 검사, 도메인 검사, Expo Doctor 18/18, iOS 번들 생성을 통과했다. Expo Go에서는 결제를 시험할 수 없으며 App Store Connect 상품과 iOS 개발 빌드가 필요하다.
- 서버 없는 로컬 판정은 위변조를 완전히 막지 못하므로 StoreKit의 현재 구매 내역과 복원 결과만으로 권한을 열고, 사진 자체는 복원하지 않는다.
- StoreKit 구매 내역 확인에 성공한 무료 사용자만 자동 정리한다. StoreKit을 쓸 수 없는 Expo Go나 조회 실패 세션은 오삭제를 막기 위해 정리를 건너뛰고 다음 실행에서 다시 시도한다.
- 자동 정리는 앱 시작, 포그라운드 복귀, 날짜 변경 뒤 실행한다. 저장·수동 삭제와 겹치지 않으며 기록 목록을 먼저 갱신한 뒤 참조되지 않은 사진을 삭제한다.
- App Store Connect 로그인과 이용약관 동의, Apple Developer의 설명 `Ootique`·명시적 번들 ID `com.lodim.ootique` 등록을 완료했다. 식별자 목록에서 등록 결과를 확인했다.
- App Store 신규 앱 창에는 iOS, 이름 `Ootique: Spin Your Style`, 기본 언어 한국어, 번들 ID `com.lodim.ootique`, SKU `ootique-ios-001`, 전체 액세스까지 입력하고 최종 `생성` 승인 대기 중이다.
- Expo 계정 `lodimsign` 로그인은 확인했지만 Ootique EAS 프로젝트는 아직 연결되지 않았다. 개인 계정과 `lodimsigns-team` 중 소유 계정을 정한 뒤 `eas init`이 필요하다.
- **2026-08-06**: lodim 승인으로 `lodimSign/ootique`를 앱 소스까지 공개했다. 8/4에 개인정보 페이지만 올려둔 저장소가 이미 있어서 그것과 합쳤다. Pages 소스를 `/docs`로 옮기고 루트 `index.html` 사본을 지웠다. `https://lodimsign.github.io/ootique/`, `#privacy`, `#support` 모두 200과 최신 내용을 확인했다. 공개 전에 전체 git 이력을 키 패턴으로 검사했고 값은 없었다(환경변수 이름만 있다).
- Pages 소스를 `/docs`로 옮기면 `docs/.nojekyll`이 있어야 한다. 없으면 Jekyll이 루트 `README.md`를 대신 렌더링해 개인정보 페이지가 사라진다 — 8/6에 실제로 그렇게 나왔다.
- 2026-08-03 재검사에서 타입 검사, 도메인·구매 판정 검사, 출시 설정 검사, Expo Doctor 18/18을 통과했다.
- EU 배포 여부와 DSA 거래자 자격 선언이 필요하다. Ootique는 인앱결제 앱이라 거래자일 가능성이 높고, 개인 거래자로 EU에 배포하면 주소·전화·이메일 공개와 검증이 필요하므로 첫 출시 EU 제외를 권장한다.
- 출시 전 사용자 직접 확인을 위해 Expo SDK 54 LAN 개발 서버를 `172.30.1.30:8081`에서 실행했고 iOS manifest 200 응답을 확인했다. Expo Go에서는 카메라·사진·기록·공유를 확인하고, 실제 StoreKit 결제는 이후 iPhone 내부 빌드에서 확인한다.
- 친구 사진 자동 연결 1차 코드와 무료 Supabase 서울 프로젝트를 배포했다. 실제 두 기기 화면 확인은 남았다.
- **2026-08-04 정정**: 그동안 "자동 검사 통과"라고 적은 `scripts/friend-sync-check.mjs`는 서버를 한 번도 호출하지 않는 정규식 검사였다. 깨진 코드에서도 통과하므로 동작을 증명하지 못한다. 실제로 서버를 호출하는 `scripts/friend-sync-e2e.mjs`를 새로 만들어 돌렸고, 서버는 방 생성·참가·승인·양방향 업로드·상대 조회·교체·권한 차단까지 전 단계 정상이었다.
- **2026-08-04 수정**: 친구 사진이 안 뜨던 원인은 앱 쪽 3건이었다. (1) `openCapture()`가 폴링이 채운 `friendUri`를 기록값(대개 null)으로 덮어써 촬영 화면에서 친구 사진이 사라졌고, `saveOotd`의 `!friendUri` 분기가 그 null을 읽어 A/B 공유 카드를 만들지 않았다. (2) 친구 사진을 기기에 받지 않고 인증 헤더가 필요한 서버 URL을 기록에 저장해, 세션이 끝나면 기록에서 열리지 않고 자동 정리의 파일 삭제 대상도 되지 못했다. (3) `Image source.headers` 의존을 없애고 폴링이 `downloadAsync`로 기기 파일에 받아 `file://` 경로만 쓰도록 바꿨다.
- **2026-08-04 정정**: `docs/app-store-metadata.md`의 개인정보 표시와 심사 메모, `docs/index.html`의 개인정보 처리방침이 친구 모드 도입 전 내용 그대로였다. "사진을 서버로 전송하지 않는다", "친구 모드는 네트워크를 쓰지 않는다"는 사실과 다르며 허위 개인정보 표시는 출시 뒤에도 앱 제거 사유다. 실제 동작(사진·기기 토큰 수집, 신원 미연결, 추적 없음)에 맞게 셋 다 고쳤다.
- 첫 실기기 진단에서 활성 회원 2명·서버 사진 0장을 확인했다. Android 화면 캡처 방식 대신 ImagePicker의 JPEG 데이터를 임시 파일로 저장해 직접 업로드하도록 수정했고 전체 검사를 통과했다.
- **2026-08-06 배포**: 공개 투표 A단계 서버가 실서버에 올라갔다. `npm run test:vote` 20개 시나리오 통과 — 한쪽만 공개하면 대결 링크가 안 생기는 것, 본인 투표 차단, 같은 쿠키 재투표 차단, 조작한 숫자 무시, 공개를 내리면 페이지·썸네일·투표가 모두 404가 되는 것, 남의 엔트리를 못 내리는 것, `purge`가 기기 토큰을 거부하는 것까지 서버에서 실제로 확인했다.
- **2026-08-06 설계 변경**: 투표 페이지를 Edge Function에서 GitHub Pages의 정적 `docs/v.html`로 옮겼다. Supabase 게이트웨이가 함수의 HTML을 `text/plain`으로 바꾸고 `CSP: default-src 'none'; sandbox`를 붙여 스크립트도 이미지도 막는다 — 피싱 방지 정책이라 우회할 수 없다. 함수는 `action=card`로 JSON만 준다. 링크가 `https://lodimsign.github.io/ootique/v.html?t=...`로 짧아진 것은 덤이다. 자세한 내용은 `C:\workspace\claude-config\mistakes.md`의 2026-08-06 항목.
- 마이그레이션 이력이 비어 있었다. `20260803113000`은 대시보드로 적용돼 있어서 `migration repair --status applied`로 이력만 맞춘 뒤 새 것만 `db push` 했다. 앞으로는 CLI로만 올린다.
- 투표자 쿠키 해시의 pepper는 `VOTE_SECRET`이고, 설정하지 않으면 `SUPABASE_SERVICE_ROLE_KEY`로 대신한다. 지금은 설정하지 않았다 — 서버 밖으로 나가지 않고 sha256 입력으로만 쓴다. 표를 초기화하고 싶으면 그때 `VOTE_SECRET`을 새로 넣으면 된다.
- 친구 기기별 토큰은 SecureStore에 저장하고 서버에는 해시만 둔다. 초대 코드는 10분 만료, 방장 승인 뒤 두 명으로 닫히며 사진 저장소는 비공개다. 투표·순위는 2차 범위다.
- **2026-08-05 수정**: B가 사진을 올리면 A에 안 뜨고 B에 `사진을 저장하지 못했어요`가 뜨던 원인은 업로드 자체가 서버에서 거절된 것이었다. 앱이 사진 선택기가 준 파일을 그대로 올렸는데, `friend-sync`는 320~4096px 진짜 JPEG만 받는다. 실서버 조사로 PNG 스크린샷과 8160×6120 원본이 각각 `400 invalid_photo`로 막히는 것을 확인했다. `expo-image-manipulator`로 올리기 전에 JPEG로 다시 인코딩하고 긴 변을 1600px로 줄인다(`toUploadJpeg`). 함께 `saveOotd`가 실패 원인을 삼키던 것도 고쳐 실기기에서 서버 거절과 기기 저장 실패를 구분할 수 있게 했다.
- 2026-08-03 두 기기 재검사에서 B의 이전 `연결 완료` 세션이 새 A 코드에도 그대로 표시돼 실제 참가 요청이 전송되지 않는 원인을 확인했다. 입력 코드와 저장된 세션 코드가 같을 때만 상태·폴링·승인·사진 접근을 허용하고, 다른 코드에는 실제 연결 버튼을 다시 보이도록 수정했다. 서버·DB 변경 없이 전체 검사와 Expo Doctor 18/18을 통과했고 두 기기 재확인이 남았다.
